# Minute Hour Day Month DayOfWeek Command
# Every hour
0 */1 * * * . /project_env.sh; /opt/venv/bin/python3 /workspace/fetch_data.py 2>&1 | tee -a ${LOG_DIR}/cron.log > /proc/1/fd/1

# Every day at 5am
# 0 5 * * * . /project_env.sh; /opt/venv/bin/python3 /workspace/fetch_data.py 2>&1 | tee -a ${LOG_DIR}/cron.log > /proc/1/fd/1

# Make sure this file ends with a newline and is using LF line endings NOT CRLF !!!
