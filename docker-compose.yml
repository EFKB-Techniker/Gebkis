version: '3'
services:
  nginx:
    build: .
    ports:
      - "8089:80"
    volumes:
      - ./app:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
